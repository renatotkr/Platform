namespace Carbon.Platform
{
    public enum ZoneLevel
    {
                         // e.g.
        Provider    = 0, // aws
        Region      = 1, // us-west1
        Datacenter  = 2, // A
        Rack        = 3  // 352
    }
}
