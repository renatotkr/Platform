namespace Carbon
{
	/*
	
	public class StreamTests
	{
		[Fact]
		public void Encrypt()
		{
			var protector = new PackageProtector("hi");

			var sourceText = "the quick brown fox ran over the road";

			using (var stream = new MemoryStream(Encoding.UTF8.GetBytes(sourceText)))
			{
				using (var encrypted = protector.Encrypt(stream))
				{
					using (var result = protector.Decrypt(encrypted))
					{

						var data = new StreamReader(result).ReadToEnd();

						Assert.Equal(data, sourceText);
					}
				}
			}

		}
	}
	*/
}
