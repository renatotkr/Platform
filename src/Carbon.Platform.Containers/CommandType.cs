namespace Carbon.Docker
{
    public enum CommandType
    {
        CMD,
        RUN,
        LABEL,
        EXPOSE,
        ENV,
        ADD,
        ENTRYPOINT,
        VOLUME,
        USER,
        WORKDIR,
        ARG,
        STOPSIGNAL,
        HEALTHCHECK,
        SHELL
    }
}
