using Carbon.Data.Expressions;
using Xunit;

namespace Carbon.Platform.Client.Tests
{
    public class ClientTests
    {
        [Fact]
        public void A()
        {
            // var expression = Expression.Eq("type", "app");

            // throw new System.Exception(expression.ToQueryString());
        }

        
    }
}