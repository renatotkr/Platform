namespace Carbon.Platform.Apps
{
    /*
    public static class AppName
    {
        // IIS
        // length = 64
        // forbidden: & / \ : * ? | " < > [ ] + = ; , @

        private static readonly char[] ForbiddenCharacters = { '&', '/', '\\', ':', '*', '?', '|', '<', '>', '[', ']', '+', '=', ';', ',', '@' };

        public static bool IsOk(string name)
        {
            // <= 50 characters
            // no forbidden characters
        }
        
    }
    */
}
