using Xunit;

namespace Carbon.Packaging.Tests
{
    public class PackageInfoTests
    {
        /*
        [Fact]
        public void FormattingTests()
        {
            var lib = new PackageInfo("cropper", new Semver(1, 2, 3));

            Assert.Equal("cropper@1.2.3", lib.ToString());
        }

        [Fact]
        public void DepTests()
        {
            var lib = new PackageDependencyInfo("a", "1.0.0");

            Assert.False(lib.IsFile);

            lib = new PackageDependencyInfo("b", "./hi.jpeg");

            Assert.True(lib.IsFile);
        }
        */
    }
}
