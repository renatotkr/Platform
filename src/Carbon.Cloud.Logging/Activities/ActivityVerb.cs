namespace Carbon.Cloud.Logging
{
    /*
    public enum ActivityVerb
    {
        // Data
        Create = 1, // Account, Domain, Bucket, Session, Subscription
        Update = 2, // Credential, Domain, Bucket 
        Delete = 3, // Domain, Bucket, Session
        Archive = 4, // ?

        Add = 5, // adding existing thing to collection
        Remove = 6, // removing existing thing from collection         

        // Interactions
        Click = 10, // Link      
        View = 11, // EmailMessage

        // Consent
        Acknowledge = 20,
        Agree = 21,
        Approve = 22,
        Authorize = 23,
        Confirm = 24,
        Disagree = 25,
        OptIn = 26, // Email
        OptOut = 27, // Email
        Retract = 28,

        // Accounting
        Close = 31, // Account
        Suspend = 32, // Account
        Verify = 33, // Account, Domain, Email

        // Commerce
        Cancel = 40, // Subscription
        Purchase = 41, // Products
        Renew = 43, // Subscription
        Refund = 44, // Invoice
        Forgive = 45, // Invoice

        // Social
        Like = 50, // *
        Dislike = 51, // *
        Favorite = 52, // *
        Share = 53, // *

        // Messaging
        Send = 60, // Message
        Receive = 62,
        Read = 63, // Message [not to be confused with view]

        // Customer Service
        Resolve = 70,

        // DevOps
        Build = 80, // Application / Frontend / Library
        Release = 81, // Application / Frontend / Library
        Deploy = 82, // Application / Frontend / Library
        Terminate = 83, // Host, Process

        Tag = 100, // tags can be flags [inappropriate]
    }
    */
}

// https://github.com/activitystreams/activity-schema/blob/master/activity-schema.md